{
    "version": 2,
    "routes": [
      { "handle": "filesystem" },
      {
        "src": "/api/(.*)",
        "dest": "server.js"
      }
    ],
    "builds": [{ "src": "server.js", "use": "@vercel/node" }]
  }
  